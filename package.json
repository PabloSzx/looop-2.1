{
  "name": "looop-monorepo",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "web:build": "pnpm build --filter @looop/web",
    "repl:build": "pnpm build --filter @looop/repl",
    "firebase-client:build": "pnpm build --filter @looop/firebase-client"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {},
  "devDependencies": {}
}
